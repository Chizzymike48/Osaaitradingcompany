{
  "name": "ossai-trading",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=ossaitrading",
    "dev:backend": "npm run dev --workspace=ossai-trading-backend",
    "build": "npm run build --workspace=ossaitrading"
  }
}
